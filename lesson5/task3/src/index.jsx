import React from 'react';
import ReactDOM from 'react-dom';
import './index.scss';
import GoodButton from './Counter.jsx';

const rootElement = document.querySelector('#root');

ReactDOM.render(<GoodButton />, rootElement);
